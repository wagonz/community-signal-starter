# Visible to services in docker-compose
POSTGRES_USER=signals
POSTGRES_PASSWORD=signals_pw
POSTGRES_DB=signals

# Frontend API targets (what the browser calls)
# For docker compose networking, backend is http://backend:8080 internally, but the browser hits Caddy on http://localhost
API_BASE_URL=http://localhost:8080
WS_URL=http://localhost:8080
